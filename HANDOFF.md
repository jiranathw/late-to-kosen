# Late to KOSEN — คู่มือส่งต่อทีม

Repo: https://github.com/jiranathw/late-to-kosen
Unity **6000.4.11f1** (2D, Built-In Render Pipeline) — ต้องเวอร์ชันนี้เป๊ะ ห้าม upgrade
Deadline: **31 ส.ค. 2026** (ไม่มีเลื่อน)

## เริ่มยังไง

```
git clone https://github.com/jiranathw/late-to-kosen.git
```

เปิดด้วย Unity Hub → Add project from disk → เลือกโฟลเดอร์ที่ clone มา
ครั้งแรก Unity จะ import สักพัก ปล่อยให้มันทำจนเสร็จ ถ้าเด้งถาม "Import TMP Essentials" ให้กด Import

เปิด scene: `Assets/Scenes/Level1.unity` — **นี่คือ scene จริงอันเดียวที่ใช้ส่งงาน**
`_Sandbox.unity` คือ scene ทดลอง จะพังยังไงก็ได้ ไม่มีผลกับงานส่ง

## ตอนนี้มีอะไรแล้ว

ระบบเกมเสร็จหมดแล้ว ทำงานได้จริง ทดสอบแล้ว:
เดิน กระโดด ชนพื้น โดนกับดัก → ตาย → เกิดใหม่ที่ checkpoint, ถึงเส้นชัย → ชนะ,
นับเวลาถอยหลังก่อนกริ่งดัง, นับจำนวนตาย, กล้องตามตัวละคร, จอชนะ/แพ้ + กด R เล่นใหม่

`Level1` มีของพร้อมใช้อยู่แล้ว: Player, GameManager, Main Camera, Canvas (Timer + Death counter), SpawnPoint
**ที่ยังไม่มีคือตัวด่าน** — พื้น กับดัก checkpoint เส้นชัย ยังไม่ได้วาง

## Prefab ที่เอาไปวางได้เลย (`Assets/Prefabs`)

| Prefab | ใช้ทำอะไร |
|---|---|
| `Ground` | พื้น/แท่นให้ยืน ลากมาวางแล้วยืดด้วย Scale ได้เลย |
| `Trap` | กับดัก โดนแล้วตาย เกิดใหม่ที่ checkpoint ล่าสุด |
| `Checkpoint1` | จุดเซฟ เดินผ่านแล้วจำตำแหน่ง |
| `Goal` | ประตูโรงเรียน แตะแล้วชนะ |

ลากจากโฟลเดอร์ Prefabs ลง Scene ได้เลย ทุกตัวต่อสายไว้หมดแล้ว **ไม่ต้องตั้งค่าอะไรเพิ่ม**

## กฎที่ห้ามแหก (ไม่งั้นพัง)

1. **ห้ามแตะ Edit → Project Settings → Tags and Layers** layer `Ground` อยู่ที่ช่อง 3 และ Player อ้างอิงเลขนี้อยู่ ถ้าไปสร้าง layer `Ground` อันใหม่ ตัวละครจะกระโดดไม่ได้ทันที และหาสาเหตุยากมาก
2. **ห้ามเปลี่ยนชื่อ class/ไฟล์ใน `Assets/Scripts`** — `GameManager`, `PlayerController`, `TrapTrigger`, `Checkpoint`, `GoalTrigger`, `CameraFollow`, `TimerUI`, `DeathCounterUI`, `ResultUI`
3. **`Level1.unity` แก้ทีละคน** Unity scene merge ไม่ได้ ถ้าสองคนแก้พร้อมกันแล้ว push ชนกัน คนหลังจะเสียงานทั้งหมด → บอกในกลุ่มก่อนทุกครั้งว่า "กูจะแก้ Level1 นะ" แล้วบอกอีกทีตอน push เสร็จ
4. **pull ก่อน push เสมอ** — `git pull` แล้วค่อย `git push`
5. **ถ้าเพิ่ม scene ใหม่ ต้องใส่ใน File → Build Settings ด้วย** ไม่งั้นปุ่ม restart พัง

## ใครทำอะไร

**Krin (ด่าน)** — วางด่านใน `Level1.unity` จาก prefab ข้างบน ให้เล่นจบได้ตั้งแต่ต้นจนถึง Goal
ตั้งเป้าเดินจบใน ~60 วินาที (timer ตั้งไว้ 90) วาง Checkpoint ทุกๆ ช่วงที่มีกับดักหนักๆ
ถ้าตกจากขอบแมพ ตัวละครจะเกิดใหม่ให้เอง (กันตกทะลุโลก)

**Bun (อาร์ต)** — เอาไฟล์ภาพลง `Assets/Art` เท่านั้น ห้ามวางที่อื่น
ส่งเป็น PNG พื้นหลังโปร่ง ตั้ง Texture Type = Sprite (2D and UI), Pixels Per Unit เท่ากันทุกไฟล์
เปลี่ยนรูปโดยลากใส่ช่อง Sprite ของ prefab ได้เลย ไม่ต้องแก้โค้ด

**TJ (โปรแกรม)** — แก้บั๊ก ต่อระบบ ทำ build

## ทำ build ยังไง

File → Build Settings → เช็คว่า `Level1` อยู่บนสุดและติ๊กอยู่ → Build
เล่นไฟล์ .exe ที่ได้ ต้องเล่นจบได้จริงนอก editor ไม่ใช่แค่ในโปรแกรม

## ถ้าเวลาไม่พอ ตัดตามลำดับนี้

1. เมนูหลัก → ใช้หน้าคำแนะนำนิ่งๆ แทน
2. อนิเมชันตัวละคร → ใช้ภาพนิ่งกลับด้านซ้ายขวาพอ
3. UI นับจำนวนตาย → ตัวหนังสือเปล่าๆ พอ
4. จำนวนกับดัก → ลดลง
5. เพลง/เสียง → ตัดทิ้งเลย

**ห้ามตัดเด็ดขาด:** เดิน/กระโดด, ตัวนับเวลา, เงื่อนไขชนะ/แพ้ ไม่มีสามอย่างนี้คือไม่มีเกมให้ส่ง
