# Late to KOSEN — คู่มือส่งต่อทีม

Repo: https://github.com/jiranathw/late-to-kosen
Unity **6000.4.11f1** (2D, Built-In Render Pipeline) — ต้องเวอร์ชันนี้เป๊ะ ห้าม upgrade
Deadline: **31 ส.ค. 2026** (ไม่มีเลื่อน) — อัปเดตล่าสุด **28 ส.ค. 2026 (ค่ำ) เหลือ 3 วัน**

---

## อ่านก่อน: เกมเหลือ 2 ด่าน และด่าน 2 คือด่านของคริน (28 ส.ค.)

ทีมโหวตกันแล้วว่า **เอาด่านที่ครินทำ** เพราะมันยากและสนุก ตอนนี้มันคือ **ด่าน 2** ด่านที่อยู่ในตึกแล้ว
และมันขึ้นเกือบทั้งดุ้นตามที่เขาวางไว้ — ทุกแท่นอยู่พิกัดเดิมทศนิยม 2 ตำแหน่ง ข้อความบนป้ายก็ของเขาคำต่อคำ
รวมพิมพ์ผิดด้วย ส่วนที่แก้มีแค่ของที่**พังจริงๆ** เขียนไว้ครบใน `Krin_tasks.md` และหัวไฟล์ `Tools/stage2.py`

**ด่าน 1 ทำใหม่ทั้งด่าน: จากหอไปตึก** — ห้องนอน ทางเดิน บันได ล็อบบี้ ซอย ถนนฉลองกรุง แล้วก็ลานหน้าตึก

**ที่เสียไปคือด่าน 3** เดิมวางไว้ 3 ด่าน แต่เหลือ 3 วัน ทำด่าน 3 ให้ดีเท่าด่านครินไม่ทัน
และด่านที่อ่อนกว่าเพื่อนจะฉุดคะแนนทั้งเกม เลยตัดด่านน้ำท่วมทิ้ง

| ด่าน | Scene | ชื่อ | เวลา | เป็นอะไร |
|---|---|---|---|---|
| 1 | `Level1.unity` | **LATE** — ห้องนอน → ประตูตึก 12 | 80 วิ | ทำใหม่ รถ จักรยานเช่า ตัวเงินตัวทอง กระถาง ถนนจริง |
| 2 | `Level2.unity` | **INSIDE** — ในตึก และมันยังไม่จบกับมึง | 65 วิ | **ของคริน** ย้ายมาแล้วซ่อมแล้ว |

**ชีวิต 3 ดวงต่อด่าน รีเซ็ตทุกด่าน** ตายหมดชีวิต = เริ่ม**ด่านนั้น**ใหม่ เสียแค่ 65-80 วินาที ไม่ใช่ทั้งเช้า
เกมไม่ได้ง่ายลง แต่มันยุติธรรมขึ้น — ปัญหาเดิมคือ checkpoint คืนตำแหน่งให้แต่ไม่คืนเลือด

**ด่าน 1 ต้องโหดเท่าด่าน 2** นั่นคือโจทย์ ไม่ใช่ tutorial อีกแล้ว ทุกมุกที่ครินใช้ในด่านเขา
ด่าน 1 ตอบกลับด้วยมุกเดียวกัน: แท่นปลอมบนหลุมที่กระโดดข้ามได้อยู่แล้ว, กับดักซ่อนที่วาง**ก่อน**ป้ายเตือน,
และประตูวาร์ปที่หน้าตาเหมือนทางลัด

---

## 3 อย่างที่เปลี่ยนในตัวละคร (สำคัญ ถ้าเคยเล่นบิลด์เก่า)

**1. ไม่มีวิ่ง (Shift) แล้ว** ความเร็ววิ่งเก่ากลายเป็นความเร็วเดียวที่มี — `moveSpeed 7.5`
เหตุผลคือหลุมแรกของด่านครินกว้าง 5.00 หน่วย ถ้าต้องกด Shift ถึงจะข้ามได้ แปลว่ามีจังหวะเดียวในเกม
ที่บังคับให้กดปุ่มพิเศษ นั่นไม่ใช่ดีไซน์ มันคือหลอด stamina ที่มีไว้แก้บั๊กเดียว — เลยเอาออกทั้งระบบ

**2. จักรยานไม่ใช่ power-up แล้ว มันคือ Anywheel** เก็บแล้ว**ขี่ตลอด** ลงเองไม่ได้
ต้องหาที่จอด (`BikeRack`) ถึงจะลงได้ ขี่แล้ว**เร็วขึ้น** (ข้ามหลุมได้ 8.11 หน่วย) แต่**กระโดดต่ำลง**
(สูงแค่ 2.17 หน่วย) แปลว่าขั้นบันไดที่เดินขึ้นสบายๆ ตอนขี่อาจขึ้นไม่ได้
ในด่าน 1 มีที่จอด 3 จุด **จุดกลางเป็นของปลอม** — generator เดินเส้นทางซ้ำทั้งเส้นด้วยฟิสิกส์ตอนขี่
และจะไม่ยอมเขียน scene ถ้าคนขี่มีโอกาสติดค้าง เพราะงั้นที่จอดปลอมคือ**เสียเวลา ไม่ใช่ softlock**

**3. หมาซอยกลายเป็นตัวเงินตัวทอง** สคริปต์เดิม เลขเดิม แต่ตลกกว่าและไทยกว่า
มีทั้งด่าน 1 (x 101) และด่าน 2 (x 27 — ของครินเดิมเป็น death beam)

---

## เริ่มยังไง

```
git clone https://github.com/jiranathw/late-to-kosen.git
```

เปิดด้วย Unity Hub → Add project from disk → เลือกโฟลเดอร์ที่ clone มา
ครั้งแรก Unity จะ import สักพัก ปล่อยให้มันทำจนเสร็จ ถ้าเด้งถาม "Import TMP Essentials" ให้กด Import

เปิด scene: **`Assets/Scenes/Level1.unity`** แล้วกด Play — ผ่านด่าน 1 แล้วมันโหลดด่าน 2 ให้เอง
ไม่ต้องลากอะไรใส่ Inspector ทั้งนั้น HUD / เมนูพัก / จอผลลัพธ์ / ป้ายชื่อด่าน สร้างตัวเองตอนรันหมด

> **หมายเหตุสำคัญ:** บิลด์ของคืนนี้ **ยังไม่เคยเปิดใน Unity editor เลยสักครั้ง**
> ทั้ง 2 scene, prefab ใหม่ 3 ตัว (`MonitorLizard`, `BikeRack`, `Bicycle` ที่สร้างใหม่)
> และสคริปต์ใหม่ 2 ตัว (`BikeRental.cs`, `BikeRack.cs`) ถูกสร้างนอก Unity ทั้งหมด
> ใครเปิดคนแรกช่วยดู console ว่าว่างจริงไหม ถ้ามี error บอกในกลุ่มก่อนทำอย่างอื่น
> **นี่คือความเสี่ยงที่เหลืออยู่จริงๆ อันเดียวของโปรเจกต์**

## เกมนี้คือเกมอะไร (อ่านก่อนแก้อะไรทั้งนั้น)

ตามฟอร์มที่ส่งอาจารย์ไป: **2D troll platformer**
คำว่า *troll* คือหัวใจของงาน ไม่ใช่ platformer ธรรมดา — ด่านต้องมีกับดักที่ "ซ่อน" อยู่
คนเล่นต้อง**ตายก่อนถึงจะรู้** แล้วรอบสองถึงจะผ่านได้ ถ้าตัดความ troll ออก = ทำผิดฟอร์ม

เงื่อนไขชนะ/แพ้ (ตามฟอร์มเป๊ะ):
- **ผ่านด่าน** = ถึงเส้นชัยของด่านนั้นก่อนหมดเวลา **และยังเหลือชีวิตอย่างน้อย 1** → ขึ้น `STAGE CLEAR`
- **ชนะจริง** = ทำแบบนั้นครบ **2 ด่าน** → ขึ้น `YOU MADE IT!` พร้อมคะแนนรวมทั้งรัน
- **แพ้** = ชีวิตหมดในด่านนั้น (`OUT OF LIVES`) **หรือ** เวลาหมด (`THE BELL RANG`)
  ทั้งสองแบบกด R = เล่น**ด่านนั้น**ใหม่ ไม่ใช่เริ่มใหม่ทั้งเกม

## GameManager กับ GameSession ต่างกันยังไง (อันนี้สำคัญ ถ้าจะเขียนโค้ดเพิ่ม)

- **`GameManager` = ระดับด่าน** ถือ นาฬิกา / ชีวิต / checkpoint / จำนวนตาย / คะแนนด่าน
  มันถูกทำลายแล้วสร้างใหม่ทุกครั้งที่โหลด scene — **นั่นแหละคือเหตุผลที่ทั้ง 3 อย่างรีเซ็ตตอนขึ้นด่านใหม่**
  ไม่ใช่ผลข้างเคียง แต่คือดีไซน์
- **`GameSession` = ระดับรัน** เป็น `DontDestroyOnLoad` อยู่ข้ามทุก scene
  ถือผลของแต่ละด่าน คะแนนรวม และเป็นคนสั่งโหลด scene

**อยากให้อะไรรีเซ็ตทุกด่าน → ใส่ `GameManager`. อยากให้อยู่ข้ามด่าน → ใส่ `GameSession`.**
ไม่มีทางเลือกที่สาม และตั้งใจให้ไม่มี

## ตอนนี้มีอะไรแล้ว

| ระบบ | อยู่ไฟล์ไหน |
|---|---|
| เดิน / กระโดด (ความเร็วเดียว ไม่มี Shift แล้ว) | `PlayerController.cs` |
| ชีวิต 3 ดวงต่อด่าน + เกิดใหม่ที่ checkpoint | `GameManager.cs` |
| คะแนน = เวลาเหลือ x10 + กับดักที่รอด x100 − ตาย x50 | `GameManager.cs` |
| คะแนนรวม 2 ด่าน + สั่งโหลดด่านถัดไป | `GameSession.cs` |
| กับดักธรรมดา (เห็นได้) | `TrapTrigger.cs` |
| **กับดักซ่อน** — มองไม่เห็น โผล่ให้เห็นถาวรหลังฆ่าครั้งแรก | `HiddenTrap.cs` |
| **แท่นปลอม** — เหยียบแล้วสั่นแล้วร่วง | `FakePlatform.cs` |
| **หนามโผล่** — ฝังในพื้น เด้งขึ้นเมื่อเข้าใกล้ | `SpikeTrap.cs` |
| **เส้นชัยปลอม** — หน้าตาเหมือนประตูโรงเรียน แตะแล้วเสียเวลา ไม่จบด่าน | `FakeGoal.cs` |
| **ป้าย** — บางป้ายบอกความจริง บางป้ายไม่ | `Signpost.cs` |
| **ตัวเงินตัวทองไล่** (ทั้ง 2 ด่าน) | `ChaserHazard.cs` |
| **กระถางตกจากระเบียง** (ด่าน 1) + **ก้อนหินร่วง** (ด่าน 2) | `FallingObject.cs` |
| **รถบนถนนฉลองกรุง** (ด่าน 1) | `TrafficLane.cs` + `Vehicle.cs` |
| **ประตูวาร์ป** — "ทางลัด" (ด่าน 1) และ secret ending (ด่าน 2) | `Teleporter.cs` |
| **จักรยานเช่า Anywheel** — ขี่จนกว่าจะเจอที่จอด | `BikeRental.cs` |
| **ที่จอดจักรยาน** — มีทั้งของจริงและของปลอม | `BikeRack.cs` |
| นับถอยหลัง 80 / 65 วินาที | `GameManager.cs` + `TimerUI.cs` |
| นับจำนวนตาย | `DeathCounterUI.cs` |
| HUD ชีวิต / คะแนน / สถานะจักรยาน | `HudUI.cs` |
| ป้ายชื่อด่านตอนเข้าด่าน | `StageBannerUI.cs` |
| จอ STAGE CLEAR / YOU MADE IT / OUT OF LIVES / THE BELL RANG | `ResultUI.cs` |
| Esc = พัก/เล่นต่อ, Q = ออกเกม | `PauseMenu.cs` |
| กล้องตามตัวละคร | `CameraFollow.cs` |

รวม **31 scripts, 16 prefabs, 102 ชิ้นวางใน 2 ด่าน**

`RisingWater.cs` กับ prefab `Floodwater` ยังอยู่ในเรโป แต่**ไม่มีด่านไหนใช้แล้ว** (ตายไปพร้อมด่าน 3)
เก็บไว้เพราะไม่เปลืองอะไร คอมไพล์ผ่าน และ generator ยังมีกฎเช็คมันอยู่เผื่อวันหลังเอากลับมา

**ของทุกชิ้นในทุกด่านเป็น Prefab Instance** แปลว่าถ้า Bun เอารูปใส่ prefab ตัวแม่
ของที่วางไว้ในทั้ง 2 ด่านเปลี่ยนตามทันทีหมด ไม่ต้องมาไล่แก้ทีละอัน

## ปุ่ม

| ปุ่ม | ทำอะไร |
|---|---|
| ลูกศรซ้าย/ขวา หรือ A / D | เดิน |
| Space หรือ ลูกศรขึ้น | กระโดด |
| — | **ไม่มีปุ่มวิ่งแล้ว** ความเร็วเดียว และมันคือความเร็ววิ่งเดิม |
| — | **ลงจักรยานเองไม่ได้** ต้องขี่ไปจนเจอที่จอด |
| Esc | พัก / เล่นต่อ |
| **Space หรือ Enter** | **ไปด่านถัดไป — เฉพาะตอนจอ `STAGE CLEAR` เท่านั้น** |
| R | เล่นด่านนี้ใหม่ (จอสุดท้ายคือเริ่มใหม่ทั้งรัน) |
| Q | ออกเกม |

Space ผูกกับ "ไปต่อ" **เฉพาะตอนผ่านด่านกลางรัน** ถ้าผูกไว้ทุกจอ คนที่กำลังรัวปุ่มกระโดดตอนตาย
จะกดข้ามจอ game over ของตัวเองไปโดยไม่ได้อ่านเลย

## ตัวเลขที่จูนไว้แล้ว (อย่าเปลี่ยนถ้าไม่มีเหตุผล)

ชุดนี้ **จูนจากด่านของครินโดยตรง** ไม่ได้จูนลอยๆ ตัวละครเดิมของเขาคือ moveSpeed 6 / jumpForce 7 /
gravityScale 1 ซึ่งลอยมาก ส่วนของเราหลังจูนรอบก่อนข้ามหลุมได้ 3.45 หน่วย
และที่ 3.45 ด่านเขาไม่ใช่ยาก มัน**เล่นไม่ได้** เพราะหลุมแรกกว้าง 5.00 หน่วย

| ค่า | เลข | เหตุผล |
|---|---|---|
| moveSpeed | 7.5 | ความเร็ววิ่งเดิม ตอนนี้คือความเร็วเดียว หลุม 5.00 หน่วยของครินต้องใช้เท่านี้ |
| jumpForce | 13.5 | คู่กับ gravityScale 2.6 |
| gravityScale | 2.6 | เดิม 1 ตัวละครลอยเหมือนอยู่บนดวงจันทร์ |
| fallMultiplier | 1.8 | ตกเร็วกว่าขึ้น ทำให้กระโดดรู้สึกกระชับ |
| bikeSpeedMultiplier | 1.5 | ขี่แล้วเร็วขึ้นชัดเจน |
| bikeJumpMultiplier | 0.78 | **ขี่แล้วกระโดดต่ำลง** นี่คือราคาของความเร็ว และเป็นที่มาของกับดักที่จอดปลอม |
| coyoteTime | 0.12 | กระโดดได้อีกนิดหลังตกขอบ คนเล่นจะไม่รู้สึกว่าเกมโกง |
| jumpBufferTime | 0.12 | กดกระโดดก่อนแตะพื้นนิดนึงก็ยังกระโดดให้ |
| startingLives | 3 **ต่อด่าน** | **ถ้าเทสแล้วยากเกิน ให้ขึ้นเป็น 5 ที่ `GameManager` ของด่านนั้น** ช่องเดียว ไม่ต้องแก้โค้ด |
| startingTime | 80 / 65 | เดินรวดเดียวไม่ตายเลยใช้ 34% / 20% ของเวลา ที่เหลือเผื่อไว้ตาย |

`sprintSpeed` กับ `maxStamina` **ถูกลบทิ้งแล้ว** ไม่ต้องไปหา

จุดสูงสุดของการกระโดด = **3.573 หน่วย** เดิน / **2.174 หน่วย** ตอนขี่จักรยาน
ระยะข้ามหลุมจริง = **6.93 หน่วย** เดิน / **8.11 หน่วย** ตอนขี่
(สั้นกว่าที่คิดเพราะ `fallMultiplier 1.8` ทำให้ตกเร็วกว่าขึ้น)

**งบที่ generator บังคับ: หลุมกว้างไม่เกิน 5.20 หน่วย ยกพื้นไม่เกิน 2.50 หน่วยตอนเดิน
และไม่เกิน 1.52 หน่วยตอนขี่จักรยาน** คำนวณใหม่ทุกครั้งที่รัน และจะไม่ยอมเขียน scene ที่ผิดกฎ

**และห้ามเหลือช่องแคบกว่า 1 หน่วยระหว่างพื้นสองชิ้นที่สูงเท่ากัน** ตัวละครกว้าง 0.5 หน่วย
ถ้าช่องแคบพอดีตัวมันจะหล่นลงไปแล้วติดค้าง ไม่ตกต่อ ไม่ตาย ออกไม่ได้ (เคยเป็นมาแล้วที่กำแพงซ้าย)

## กฎการวางกับดัก troll (สำคัญมาก อ่านก่อนวางเพิ่ม)

เกม troll ที่ดีคือ **โหดแต่ยุติธรรม** ไม่ใช่โหดแบบมั่ว
เส้นแบ่งคือ: ตายแล้วรู้ไหมว่ารอบหน้าต้องทำอะไรต่างไป ถ้าไม่รู้ = ไม่ยุติธรรม ไม่ใช่ troll

1. **กับดักซ่อน (`TrapHidden`) โผล่ให้เห็นถาวรหลังฆ่าครั้งแรก** ตายรอบเดียวพอ รอบสองต้องเห็นแล้ว
   และห้ามวางสองอันติดกันจนรอดอันแรกแล้วตกอันที่สอง
2. **แท่นปลอม (`PlatformFake`) ต้องวางกลางหลุมที่กระโดดข้ามได้อยู่แล้วโดยไม่ต้องเหยียบ**
   มันคือ*สิ่งล่อใจ* ไม่ใช่*ทางเดียว*
3. **หนามโผล่ (`TrapSpike`) ต้องให้เวลาตอบสนอง** ระยะ trigger 2.2 หน่วย = ประมาณ 290ms ที่ความเร็ว 7.5
4. **ห้ามวางกับดักติดขอบแท่น** ต้องมีที่วิ่งขึ้นอย่างน้อย 1 หน่วยก่อนถึงกับดัก และเหลือขอบท้ายอย่างน้อย 0.5 หน่วย
5. **ห้ามวางกับดักใกล้ checkpoint ต่ำกว่า 1.2 หน่วย** ไม่งั้นเกิดใหม่แล้วตายซ้ำวนไม่จบ
6. **จักรยานวางบนที่โล่งเท่านั้น** ห้ามวางก่อนกับดักภายใน 8 หน่วย
7. **เวลาเดินรวดเดียวต้องไม่เกิน 75% ของนาฬิกาด่าน** เหลือที่ให้ตายจริงๆ
8. **เส้นทางหลังจักรยานต้องขี่ผ่านได้ทั้งเส้น** generator เดินซ้ำจากจักรยานตัวแรกถึงเส้นชัย
   ด้วยฟิสิกส์ตอนขี่ (หลุม ≤ 6.08 / ยกพื้น ≤ **1.52**) ถ้ามีจุดไหนคนขี่ติดค้าง มันจะไม่เขียนไฟล์เลย
   **กฎข้อนี้คือเหตุผลที่กล้าวางที่จอดปลอม**

`Tools/build_levels.py` เช็คกฎพวกนี้ให้หมดแล้ว ตอนนี้ผ่าน **0 fail** เหลือ 5 warning ที่ตั้งใจ (ข้างล่าง)

## ฉากใหญ่ กับเลขที่อยู่เบื้องหลัง

จับเวลามาด้วยมือแล้ว ถ้ารู้สึกว่าอันไหนพัง ให้อ้างเลขกลับมา อย่าเดา

**ตัวเงินตัวทอง (ด่าน 1 x 101, ด่าน 2 x 27)** ตื่นเมื่อเข้าใกล้ 9 หน่วย ไล่ที่ความเร็ว 7.0-7.2
(ช้ากว่าเรานิดเดียว เราคือ 7.5) เลิกไล่หลังจาก 24-30 หน่วย เดินเต็มสปีดก็รอด เหลือระยะไม่กี่หน่วย
**มันมีไว้ให้ตกใจ ไม่ได้มีไว้ให้ตาย** ถ้าเทสแล้วคนตายเพราะมันบ่อย = มันเร็วเกิน

**กระถางต้นไม้ (ด่าน 1, x 121 กับ x 126)** ตกที่ gravity 3.4 แล้วลงหัวพอดีที่ความเร็วเดิน
อันแรกมีป้ายเตือน **อันที่สองไม่มี** — นั่นคือมุกทั้งหมดของฉากนี้
ถ้าเทสแล้วคนไม่ตกใจอันที่สอง แปลว่ามุกไม่เวิร์ก ให้ย้าย ไม่ใช่ให้ลบ

**ถนนฉลองกรุง (ด่าน 1, x 140-166)** 2 เลน วิ่งมาทางซ้ายทั้งคู่ แต่คนละจังหวะ:
มอเตอร์ไซค์ทุก 2.0 วิ ที่ความเร็ว 6.5 / กระบะทุก 2.8 วิ ที่ความเร็ว 4.5 เหลื่อมกัน 1.3 วิ
**ถนนจะไม่มีวันตันพร้อมกันทั้งสองเลน** และมีเกาะกลางยืนได้ที่ x 151.5-154.5
มันคือปริศนาจังหวะ ไม่ใช่วัดรีเฟล็กซ์

**ประตูวาร์ป "ทางลัด" (ด่าน 1, x 118)** ทำงาน**ครั้งเดียวต่อการโหลดด่าน** ส่งกลับไปที่ x 84
คือถอยหลัง 34 หน่วย **แถมลาก checkpoint กลับไปด้วย** เพื่อไม่ให้กดหนีได้
**ตั้งใจไม่ให้มัน re-arm ตอนเกิดใหม่** เพราะทางออกมันอยู่*หลัง*ทางเข้า ถ้าให้มันทำงานซ้ำ
คนเล่นจะโดนจับวนไปมาตลอด ไม่เสียชีวิต แต่ก็ออกไม่ได้และชนะไม่ได้
troll ที่เดินหนีไม่ได้ ไม่ใช่ troll มันคือบั๊ก — **อย่าขอให้มันทำงานซ้ำ** (ประตู secret ending ของครินก็เหตุผลเดียวกัน)

**หลุมของคริน (ด่าน 2, ใต้ Ledge_3 → Ledge_4)** กระโดดพลาดแล้วไม่ตาย — ตกลงไป 3 ชั้น
แล้ว**ปีนออกไม่ได้** ข้างล่างมีประตูวาร์ป 2 บาน หน้าตาเหมือนกันเป๊ะ บานนึงคือ secret ending
ที่ส่งกลับจุดเกิดพร้อมลาก checkpoint ไปด้วย อีกบานคือลิฟต์พนักงานกลับขึ้นสนาม **แยกไม่ออก**
อันนี้เป็นมุกที่ดีที่สุดในเกมและเป็นของครินล้วนๆ **ห้ามแตะ**

## warning ที่เราตั้งใจเก็บไว้

**`Level1: 39u between checkpoints around x=133`** — ปล่อยไว้ พื้นถัดไปคือถนนฉลองกรุง
ถ้าเอา checkpoint ไปวางบนถนน เกิดใหม่แล้วรถชนทันที เดินย้อน 5 วินาทีแย่ก็จริง
แต่เกิดใหม่แล้วตายโดยไม่ได้กดอะไรเลยแย่กว่า

**`Level2: step-up is N% of apex` 4 อัน (59%, 56%, 65%, 56%)** — เป็นชั้นวางของครินทั้งหมด
มันเตือนเมื่อเกิน 55% และจะ fail เมื่อเกิน 70% ตั้งใจให้ต้องกระโดดเฉียดยอดโค้ง **ปล่อยไว้**

## Prefab ที่เอาไปวางได้เลย (`Assets/Prefabs`)

| Prefab | ใช้ทำอะไร |
|---|---|
| `Ground` | พื้น/แท่นให้ยืน ยืดด้วย Scale ได้ |
| `Trap` | กับดักธรรมดา เห็นได้ โดนแล้วเสียชีวิต 1 |
| `TrapHidden` | **กับดักซ่อน** ในเกมมองไม่เห็น จะเห็นก็ต่อเมื่อมันฆ่าไปแล้ว (ใน editor เห็นเป็นสีแดง) |
| `TrapSpike` | **หนามโผล่** ฝังในพื้น เด้งขึ้นเมื่อคนเล่นเข้าใกล้ |
| `PlatformFake` | **แท่นปลอม** หน้าตาเหมือน `Ground` เป๊ะ เหยียบแล้วสั่น 0.35 วิ แล้วร่วง |
| `FakeGoal` | **เส้นชัยปลอม** หน้าตาเหมือน `Goal` เป๊ะ แตะแล้วเสียเวลา 2-3 วิ ขึ้น OUT OF ORDER |
| `Signpost` | ป้าย ข้อความตั้งใน Inspector (ตัวหนังสือวาดเองในโค้ด) |
| `MonitorLizard` | ตัวเงินตัวทอง ตื่นแล้วไล่ แล้วเลิกไล่ (เดิมชื่อ `Dog`) |
| `Flowerpot` | ของที่ร่วงลงมา — กระถางในด่าน 1 ก้อนหินในด่าน 2 |
| `TrafficLane` | เลนรถ ปล่อยรถออกมาเองเป็นระยะ |
| `Teleporter` | ประตูวาร์ป ทำงานครั้งเดียวต่อการโหลดด่าน |
| `Bicycle` | **จักรยานเช่า** เก็บแล้วขี่ตลอด ลงเองไม่ได้ |
| `BikeRack` | **ที่จอดจักรยาน** ช่อง `isReal` ใน Inspector บอกว่าจอดได้จริงหรือหลอก |
| `Checkpoint1` | จุดเซฟ เดินผ่านแล้วเปลี่ยนเป็นสีเขียว = จำตำแหน่งแล้ว |
| `Goal` | เส้นชัยของด่าน แตะแล้วผ่านด่าน |
| `Floodwater` | น้ำท่วม **ไม่มีด่านไหนใช้แล้ว** เก็บไว้เฉยๆ |

ทุกตัวต่อสายไว้หมดแล้ว **ไม่ต้องตั้งค่าอะไรเพิ่ม**
**`PlatformFake` ต้องอยู่ layer `Ground` (ช่อง 3) เท่านั้น** ไม่งั้นเหยียบไม่ได้ตั้งแต่แรกแล้วมันจะไม่ troll

## เครื่องมือสร้างด่าน

```
python3 Tools/build_levels.py --check    # เช็คกฎดีไซน์ ไม่เขียนไฟล์
python3 Tools/build_levels.py            # สร้างใหม่ทั้ง 2 ด่าน + Build Settings
python3 Tools/verify_scenes.py           # เช็คโครงสร้างไฟล์ scene ที่เขียนออกมา
```

**ต้องรันทั้งสองตัว** มันเช็คคนละเรื่อง ไม่ทับกัน:
`build_levels.py` เช็คว่าด่าน**เล่นได้ไหม** (ระยะกระโดด ช่องติด กับดักใกล้ checkpoint ขี่จักรยานติดไหม ฯลฯ)
`verify_scenes.py` เช็คว่าไฟล์**เป็น Unity scene ที่ถูกต้องไหม** (anchor ซ้ำ อ้างอิงลอย guid ไม่มีจริง
`.meta` หาย ลำดับ Build Settings) ด่านผ่านตัวนึงแต่ตกอีกตัวได้

**ตอนนี้ generator แยกเป็น 3 ไฟล์แล้ว** อ่านง่ายกว่าเดิมเยอะ:

| ไฟล์ | มีอะไร |
|---|---|
| `Tools/level_kit.py` | ค่าฟิสิกส์ กฎดีไซน์ และฟังก์ชันวางของบรรทัดเดียว (`ground`, `trap`, `hidden`, `lizard`, `bicycle`, `bike_rack`, …) |
| `Tools/stage1.py` | ตำแหน่งของทุกอย่างในด่าน 1 พร้อมคอมเมนต์อธิบายทุกจังหวะ |
| `Tools/stage2.py` | **ด่านของคริน** พิกัดของเขา ป้ายของเขา และ 6 ข้อที่แก้ เขียนไว้หัวไฟล์ |

**แก้ตำแหน่งของในด่านที่ `stage1.py` / `stage2.py` แล้วรันใหม่ = ทางที่รองรับ**
**แก้ใน Unity Inspector = หายทันทีที่มีคนรัน generator ครั้งถัดไป**

ยกเว้น 2 ค่านี้ แก้ใน Inspector ได้ปลอดภัย: `startingTime` กับ `startingLives` ของ `GameManager` ในด่านนั้น

`Tools/build_level.py` (ไม่มี s) คือตัวเก่าของด่านเดียว **เลิกใช้แล้ว อย่ารัน**

## กฎที่ห้ามแหก (ไม่งั้นพัง)

1. **ห้ามแตะ Edit → Project Settings → Tags and Layers** layer `Ground` อยู่ที่ช่อง 3 และทั้ง
   Player และ `PlatformFake` อ้างอิงเลขนี้อยู่ ถ้าไปสร้าง layer `Ground` อันใหม่
   ตัวละครจะกระโดดไม่ได้ทันที และหาสาเหตุยากมาก
2. **ห้ามเปลี่ยนชื่อ class/ไฟล์ใน `Assets/Scripts`**
3. **scene แก้ทีละคน** Unity scene merge ไม่ได้ ถ้าสองคนแก้พร้อมกันแล้ว push ชนกัน คนหลังเสียงานหมด
   → บอกในกลุ่มก่อนทุกครั้งว่า "กูจะแก้ Level2 นะ" แล้วบอกอีกทีตอน push เสร็จ
   (ทางที่ดีกว่าคือส่งตำแหน่ง x ให้ TJ แก้ในตาราง แล้ว generate ใหม่)
4. **pull ก่อน push เสมอ**
5. **ถ้าเพิ่ม scene ใหม่ ต้องใส่ใน File → Build Settings ด้วย** ตอนนี้มี **2 ด่าน** ติ๊กครบ เรียงถูกแล้ว
   ถ้าลำดับเพี้ยน ปุ่มไปด่านถัดไปจะพัง และในeditor มันจะเงียบ ไม่ฟ้อง
   (`Level3.unity` ถูกลบไปแล้วพร้อม `.meta` ถ้าเห็นมันโผล่มาแปลว่ามีคน pull ของเก่ามาทับ)
6. **ห้ามลบ `Ground_Wall_Left` ของด่านไหนก็ตาม** เป็นกำแพงกันคนเล่นเดินย้อนออกนอกแมพ
7. **ข้อความในเกมต้องเป็นภาษาอังกฤษเท่านั้น** ฟอนต์ TMP ที่มากับ Unity (LiberationSans) ไม่มีตัวอักษรไทย
   ใส่ไทยไปจะขึ้นเป็นสี่เหลี่ยมเปล่า ถ้าอยากได้ไทยต้อง import ฟอนต์ไทยเป็น TMP Font Asset ก่อน

## ใครทำอะไร (เหลือ 3 วัน)

**TJ (โปรแกรม)** — เปิดทั้ง 2 scene ใน Unity ให้ผ่านก่อน นี่คือด่านแรกและเป็นตัวบล็อกทุกอย่าง
แล้วเดินเช็คลิสต์ใน `TJ_tasks.md` แล้วค่อย commit
**ห้ามแก้ไฟล์ใน `Assets/Scripts` หรือ `Tools` ผ่าน shell/สคริปต์ฝั่ง Linux ถ้าไฟล์นั้นเพิ่งถูกเขียนจากฝั่ง Windows**
— mount มันเห็นไฟล์ไม่ครบ (ขาดท้าย) ใครเขียนทับผ่านทางนั้นไฟล์จะขาดเงียบๆ โดนมาแล้ว 2 รอบ

**Krin (ด่าน + PM)** — เล่นทั้ง 2 ด่านหลายๆ รอบ **และเช็ค 6 ข้อที่เราแก้ในด่านมึง** (อยู่ใน `Krin_tasks.md`)
จดว่าตรงไหนน่าเบื่อ ตรงไหนรู้สึกโดนโกง ตรงไหนตายเกิน 2 ครั้งที่จุดเดิม
**ลองขี่จักรยานให้ตัวเองติดค้างให้ได้** ถ้าทำได้ นั่นคือบั๊กอันดับ 1 ของโปรเจกต์
**แล้วหาคนที่ไม่เคยเล่นมาเล่นให้ดู ห้ามช่วย ห้ามอธิบาย**
ดูว่าเขาหัวเราะหรือปิดเกมหนี — เกม troll วัดกันที่ตรงนี้อย่างเดียว และวัดกับตัวเองไม่ได้
**ส่งกลับมาเป็นตำแหน่ง x + ปัญหา** เช่น "Level1 x≈160 เลนรถที่สอง ตาย 6 ครั้ง รู้สึกมั่ว"

**Bun (อาร์ต + เสียง)** — เริ่มได้เมื่อ layout นิ่งแล้ว งานคือ **ฉากหลัง 2 ฉาก, SFX, BGM**
เอาไฟล์ภาพลง `Assets/Art` เท่านั้น
**Texture Type = Sprite (2D and UI)**, **Filter Mode = Point (no filter)**, **Compression = None**,
**Pixels Per Unit เท่ากันทุกไฟล์**
เปลี่ยนรูปโดยลากใส่ช่อง Sprite ของ **prefab ตัวแม่** ของในทุกด่านเปลี่ยนตามหมดเอง
**`PlatformFake` ต้องใช้รูปเดียวกับ `Ground` เป๊ะ และ `FakeGoal` ต้องใช้รูปเดียวกับ `Goal` เป๊ะ**
ถ้าดูออกว่าอันไหนปลอม มันก็ไม่ troll แล้ว
**และที่จอดจักรยานปลอมต้องเหมือนของจริงเป๊ะเช่นกัน** รายละเอียดอยู่ใน `Bun_tasks.md`

## ทำ build ยังไง

File → Build Settings → เช็คว่ามี **2 scene** ติ๊กครบ และ `Level1` อยู่บนสุด (index 0) → Build
เกมออกมาเป็นหน้าต่าง 1280x720 (ไม่ใช่เต็มจอ)
**เล่นไฟล์ .exe ที่ได้ให้จบทั้ง 2 ด่านจริงๆ** โดยเฉพาะรอยต่อระหว่างด่าน —
ใน editor มันทำงานได้แม้ scene ไม่อยู่ใน Build Settings แต่ใน build มันพังเงียบๆ

## ถ้าเวลาไม่พอ ตัดตามลำดับนี้

1. เมนูหลัก / หน้าคำแนะนำ (ยังไม่ได้เริ่ม)
2. อนิเมชันตัวละคร → ใช้ภาพนิ่งกลับด้านซ้ายขวาพอ (โค้ดกลับด้านให้อยู่แล้ว)
3. BGM → เก็บ SFX ไว้ก่อน เสียงตายกับเสียงผ่านด่านสำคัญกว่าเพลง
4. ฉากหลัง → ใช้สีพื้นแทนได้ 1 ฉาก แต่ห้ามทั้ง 2 ฉาก
5. ระบบจักรยาน → ลบจักรยานกับที่จอดออกจาก `stage1.py` ระบบอื่นไม่พึ่งมัน (แต่เสียมุกที่บุ้นขอไป)
6. จำนวนกับดัก → ลดกับดักธรรมดาก่อน เก็บกับดัก troll ไว้

**ห้ามตัดเด็ดขาด:** เดิน/กระโดด, ตัวนับเวลา, ระบบชีวิต, ระบบคะแนน, กับดัก troll, เงื่อนไขชนะ/แพ้
หกอย่างนี้เขียนไว้ในฟอร์มที่ส่งอาจารย์แล้ว

**และห้ามตัดกลับไปเหลือด่านเดียว** เพราะ "multiple levels" เป็น optional feature ที่เราเคลมไปแล้ว
2 ด่านคือขั้นต่ำ ไม่ใช่เป้าหมาย
