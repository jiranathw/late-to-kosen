# Late to KOSEN — คู่มือส่งต่อทีม

Repo: https://github.com/jiranathw/late-to-kosen
Unity **6000.4.11f1** (2D, Built-In Render Pipeline) — ต้องเวอร์ชันนี้เป๊ะ ห้าม upgrade
Deadline: **31 ส.ค. 2026** (ไม่มีเลื่อน) — อัปเดตล่าสุด 26 ส.ค. 2026

## เริ่มยังไง

```
git clone https://github.com/jiranathw/late-to-kosen.git
```

เปิดด้วย Unity Hub → Add project from disk → เลือกโฟลเดอร์ที่ clone มา
ครั้งแรก Unity จะ import สักพัก ปล่อยให้มันทำจนเสร็จ ถ้าเด้งถาม "Import TMP Essentials" ให้กด Import

เปิด scene: `Assets/Scenes/Level1.unity` — **นี่คือ scene จริงอันเดียวที่ใช้ส่งงาน**
`_Sandbox.unity` คือ scene ทดลอง จะพังยังไงก็ได้ ไม่มีผลกับงานส่ง

## เกมนี้คือเกมอะไร (อ่านก่อนแก้อะไรทั้งนั้น)

ตามฟอร์มที่ส่งอาจารย์ไป: **2D troll platformer**
คำว่า *troll* คือหัวใจของงาน ไม่ใช่ platformer ธรรมดา — ด่านต้องมีกับดักที่ "ซ่อน" อยู่
คนเล่นต้อง**ตายก่อนถึงจะรู้** แล้วรอบสองถึงจะผ่านได้ ถ้าตัดความ troll ออก = ทำผิดฟอร์ม

เงื่อนไขชนะ/แพ้ (ตามฟอร์มเป๊ะ):
- **ชนะ** = ถึงโรงเรียนก่อนหมดเวลา **และยังเหลือชีวิตอย่างน้อย 1**
- **แพ้** = ชีวิตหมด (โดนกับดักครบ 3 ครั้ง) **หรือ** นับถอยหลังถึง 0

## ตอนนี้มีอะไรแล้ว

**เกมเล่นจบได้แล้วตั้งแต่ต้นจนถึงเส้นชัย** ไม่ต้องวางอะไรเพิ่มก็ส่งได้

ระบบครบตามฟอร์ม:

| ระบบ | อยู่ไฟล์ไหน |
|---|---|
| เดิน / กระโดด / วิ่ง (Shift) | `PlayerController.cs` |
| ชีวิต 3 ดวง + เกิดใหม่ที่ checkpoint | `GameManager.cs` |
| คะแนน = เวลาเหลือ x10 + กับดักที่รอด x100 − ตาย x50 | `GameManager.cs` |
| กับดักธรรมดา (เห็นได้) | `TrapTrigger.cs` |
| **กับดักซ่อน** — มองไม่เห็น โผล่ให้เห็นถาวรหลังฆ่าครั้งแรก | `HiddenTrap.cs` |
| **แท่นปลอม** — เหยียบแล้วสั่นแล้วร่วง | `FakePlatform.cs` |
| **หนามโผล่** — ฝังในพื้น เด้งขึ้นเมื่อเข้าใกล้ 2.2 หน่วย | `SpikeTrap.cs` |
| จักรยาน (power-up) — เร็วขึ้น 45% เป็นเวลา 4.5 วินาที | `BicyclePickup.cs` |
| นับถอยหลัง 85 วินาที (เสียงกริ่ง) | `GameManager.cs` + `TimerUI.cs` |
| นับจำนวนตาย | `DeathCounterUI.cs` |
| HUD ชีวิต / คะแนน / หลอดวิ่ง | `HudUI.cs` |
| จอชนะ/แพ้ + กด R เล่นใหม่ | `ResultUI.cs` |
| Esc = พัก/เล่นต่อ, Q = ออกเกม | `PauseMenu.cs` |
| กล้องตามตัวละคร | `CameraFollow.cs` |

**UI ทั้งหมดสร้างตัวเองตอนรัน** (`HudUI`, `PauseMenu`, `ResultUI`) ไม่ต้องลากอะไรใส่ scene
ไม่มีช่องให้ใครเผลอ unassign ใน Inspector = พังไม่ได้

ด่านใน `Level1`: พื้น 13 ชิ้น, กับดักเห็นได้ 12, กับดักซ่อน 5, หนามโผล่ 3, แท่นปลอม 3,
จักรยาน 2, checkpoint 5, ประตูโรงเรียน 1
ด่านยาว x = -7 ถึง 156 เส้นชัยที่ x = 152 วิ่งรวดเดียวไม่หยุด ~27 วินาที เล่นจริงครั้งแรก ~50-70 วินาที

**ของทุกชิ้นในด่านเป็น Prefab Instance** แปลว่าถ้า Bun เอารูปใส่ prefab ตัวแม่ ของที่วางไว้ในด่านเปลี่ยนตามทันทีทั้งหมด ไม่ต้องมาไล่แก้ทีละอัน

## ปุ่ม (ตามฟอร์ม)

| ปุ่ม | ทำอะไร |
|---|---|
| ลูกศรซ้าย/ขวา หรือ A / D | เดิน |
| Space หรือ ลูกศรขึ้น | กระโดด |
| Shift | วิ่ง (มีหลอด stamina 2.5 วินาที ไม่ใช่วิ่งฟรีตลอด) |
| Esc | พัก / เล่นต่อ |
| R | เริ่มด่านใหม่ (ตอนพัก หรือตอนจบเกม) |
| Q | ออกเกม (ตอนพัก) |

## ตัวเลขที่จูนไว้แล้ว (อย่าเปลี่ยนถ้าไม่มีเหตุผล)

| ค่า | เลข | เหตุผล |
|---|---|---|
| moveSpeed | 6 | เคยลองขึ้นเป็น 8 แล้วเล่นจริงรู้สึกเหมือนวิ่งตลอดเวลา คุมยาก เลยลงกลับมาที่ 6 |
| sprintSpeed | 8.5 | Shift แล้วเร็วขึ้นชัดเจน แต่ยังหยุดทัน |
| maxStamina | 2.5 | วิ่งได้ 2.5 วินาทีแล้วต้องปล่อย ไม่งั้น moveSpeed ไม่มีความหมาย |
| jumpForce | 13 | คู่กับ gravityScale 3 |
| gravityScale | 3 | เดิม 1 ตัวละครลอยเหมือนอยู่บนดวงจันทร์ |
| fallMultiplier | 1.5 | ตกเร็วกว่าขึ้น ทำให้กระโดดรู้สึกกระชับ |
| coyoteTime | 0.12 | กระโดดได้อีกนิดหลังตกขอบ คนเล่นจะไม่รู้สึกว่าเกมโกง |
| jumpBufferTime | 0.12 | กดกระโดดก่อนแตะพื้นนิดนึงก็ยังกระโดดให้ |
| startingLives | 3 | ตามฟอร์ม **ถ้าเทสแล้วยากเกินจนเล่นไม่จบ ให้ขึ้นเป็น 5 ที่ GameManager ในด่าน** |
| startingTime | 85 | วิ่งรวดเดียวไม่ตายเลย ~27 วินาที เหลือเผื่อตายอีก ~58 วินาที |

จุดสูงสุดของการกระโดด = 2.87 หน่วย ระยะข้ามหลุมไกลสุด = **4.81 หน่วย** (ตอนยกพื้น 1.5 หน่วยเหลือ 4.15)
**ทุกหลุมในด่านกว้าง 3 หน่วยหรือน้อยกว่า** เผื่อไว้อย่างน้อย 1.15 หน่วยให้คนเล่นครั้งแรกข้ามได้
ถ้าจะขยับพื้นหรือเพิ่มหลุม **ห้ามเกิน 3 หน่วย** และห้ามยกพื้นสูงขึ้นเกิน 1.5 หน่วยต่อครั้ง

**และห้ามเหลือช่องแคบกว่า 1 หน่วยระหว่างพื้นสองชิ้นที่สูงเท่ากัน** ตัวละครกว้าง 0.5 หน่วย ถ้าช่องแคบพอดีตัวมันจะหล่นลงไปแล้วติดค้าง ไม่ตกต่อ ไม่ตาย ออกไม่ได้ (เคยเป็นมาแล้วที่กำแพงซ้าย)

## กฎการวางกับดัก troll (สำคัญมาก อ่านก่อนวางเพิ่ม)

เกม troll ที่ดีคือ **โหดแต่ยุติธรรม** ไม่ใช่โหดแบบมั่ว กติกาที่ใช้วางของในด่านตอนนี้:

1. **กับดักซ่อน (`TrapHidden`) โผล่ให้เห็นถาวรหลังฆ่าครั้งแรก** ตายรอบเดียวพอ รอบสองต้องเห็นแล้ว
2. **แท่นปลอม (`PlatformFake`) ต้องวางกลางหลุมที่กระโดดข้ามได้อยู่แล้วโดยไม่ต้องเหยียบ**
   มันคือ*สิ่งล่อใจ* ไม่ใช่*ทางเดียว* ถ้าคนเล่นไม่เหยียบมันเลยก็ต้องผ่านได้ และมันจะหายไปหลังร่วง
3. **หนามโผล่ (`TrapSpike`) ต้องให้เวลาตอบสนอง** ระยะ trigger 2.2 หน่วย = เดิน 367ms / วิ่ง 259ms
4. **ห้ามวางกับดักติดขอบแท่น** ต้องมีที่วิ่งขึ้นอย่างน้อย 1 หน่วยก่อนถึงกับดัก และเหลือขอบท้ายอย่างน้อย 0.5 หน่วย
5. **ห้ามวางกับดักใกล้ checkpoint ต่ำกว่า 1.2 หน่วย** ไม่งั้นเกิดใหม่แล้วตายซ้ำวนไม่จบ
6. **จักรยานวางบนที่โล่งเท่านั้น** ห้ามวางก่อนกับดักภายใน 8 หน่วย มันมีไว้ซื้อเวลาคืน ไม่ใช่กับดักแฝง

`Tools/build_level.py` มี verifier เช็คกฎพวกนี้ให้หมดแล้ว ตอนนี้ผ่าน 0 fail 0 warning

## Prefab ที่เอาไปวางได้เลย (`Assets/Prefabs`)

| Prefab | ใช้ทำอะไร |
|---|---|
| `Ground` | พื้น/แท่นให้ยืน ลากมาวางแล้วยืดด้วย Scale ได้เลย |
| `Trap` | กับดักธรรมดา เห็นได้ โดนแล้วเสียชีวิต 1 |
| `TrapHidden` | **กับดักซ่อน** ในเกมมองไม่เห็น จะเห็นก็ต่อเมื่อมันฆ่าไปแล้ว (ใน editor เห็นเป็นสีแดง) |
| `TrapSpike` | **หนามโผล่** ฝังในพื้น เด้งขึ้นเมื่อคนเล่นเข้าใกล้ |
| `PlatformFake` | **แท่นปลอม** หน้าตาเหมือน `Ground` เป๊ะ เหยียบแล้วสั่น 0.35 วิ แล้วร่วง |
| `Bicycle` | จักรยาน เก็บแล้วเร็วขึ้น 45% เป็นเวลา 4.5 วินาที (กลับมาใหม่ใน 6 วินาที) |
| `Checkpoint1` | จุดเซฟ เดินผ่านแล้วเปลี่ยนเป็นสีเขียว = จำตำแหน่งแล้ว |
| `Goal` | ประตูโรงเรียน แตะแล้วชนะ |

ลากจากโฟลเดอร์ Prefabs ลง Scene ได้เลย ทุกตัวต่อสายไว้หมดแล้ว **ไม่ต้องตั้งค่าอะไรเพิ่ม**

**`PlatformFake` ต้องอยู่ layer `Ground` (ช่อง 3) เท่านั้น** ไม่งั้นเหยียบไม่ได้ตั้งแต่แรกแล้วมันจะไม่ troll

## กฎที่ห้ามแหก (ไม่งั้นพัง)

1. **ห้ามแตะ Edit → Project Settings → Tags and Layers** layer `Ground` อยู่ที่ช่อง 3 และทั้ง Player และ `PlatformFake` อ้างอิงเลขนี้อยู่ ถ้าไปสร้าง layer `Ground` อันใหม่ ตัวละครจะกระโดดไม่ได้ทันที และหาสาเหตุยากมาก
2. **ห้ามเปลี่ยนชื่อ class/ไฟล์ใน `Assets/Scripts`**
3. **`Level1.unity` แก้ทีละคน** Unity scene merge ไม่ได้ ถ้าสองคนแก้พร้อมกันแล้ว push ชนกัน คนหลังจะเสียงานทั้งหมด → บอกในกลุ่มก่อนทุกครั้งว่า "กูจะแก้ Level1 นะ" แล้วบอกอีกทีตอน push เสร็จ
4. **pull ก่อน push เสมอ** — `git pull` แล้วค่อย `git push`
5. **ถ้าเพิ่ม scene ใหม่ ต้องใส่ใน File → Build Settings ด้วย** ไม่งั้นปุ่ม restart พัง
6. **ห้ามลบ `Ground_Wall_Left`** เป็นกำแพงกันคนเล่นเดินย้อนออกนอกแมพ
7. **ข้อความในเกมต้องเป็นภาษาอังกฤษเท่านั้น** ฟอนต์ TMP ที่มากับ Unity (LiberationSans) ไม่มีตัวอักษรไทย ใส่ไทยไปจะขึ้นเป็นสี่เหลี่ยมเปล่า ถ้าอยากได้ไทยต้อง import ฟอนต์ไทยเป็น TMP Font Asset ก่อน

## ใครทำอะไร

**Krin (ด่าน)** — ด่านวางเสร็จแล้วเล่นจบได้ งานที่เหลือคือ *ทำให้มัน troll ขึ้นแต่ยังผ่านได้*
เล่นจริงหลายๆ รอบ แล้วดูว่าตรงไหนน่าเบื่อ ตรงไหนโหดเกิน ขยับกับดัก เพิ่มแท่นเล็กๆ ให้มีจังหวะ
**อ่าน "กฎการวางกับดัก troll" ข้างบนก่อนวางอะไรเพิ่ม** กติกาแข็ง: หลุมห้ามเกิน 3 หน่วย ยกพื้นห้ามเกิน 1.5 หน่วย

**Bun (อาร์ต)** — เอาไฟล์ภาพลง `Assets/Art` เท่านั้น ห้ามวางที่อื่น
ส่งเป็น PNG พื้นหลังโปร่ง แล้วตั้งค่าใน Inspector ให้ครบ 3 อย่าง:
**Texture Type = Sprite (2D and UI)**, **Filter Mode = Point (no filter)**, **Compression = None**
(สองอันหลังสำคัญมาก ถ้าไม่ตั้ง pixel art จะเบลอ ไม่เป็น 8-bit ตามฟอร์ม)
และ **Pixels Per Unit ต้องเท่ากันทุกไฟล์** ไม่งั้นของแต่ละชิ้นจะสเกลไม่ตรงกัน
เปลี่ยนรูปโดยลากใส่ช่อง Sprite ของ **prefab ตัวแม่** ใน `Assets/Prefabs` ของในด่านจะเปลี่ยนตามหมดเอง ไม่ต้องแก้โค้ด ไม่ต้องแตะ scene
ที่ต้องวาด: ตัวละคร, พื้น/แท่น, กับดัก, หนาม, จักรยาน, ประตูโรงเรียน, ฉากหลัง
**`PlatformFake` ต้องใช้รูปเดียวกับ `Ground` เป๊ะ** ถ้าดูออกว่าอันไหนปลอม มันก็ไม่ troll แล้ว

**TJ (โปรแกรม)** — แก้บั๊ก ต่อระบบ ทำ build

## ทำ build ยังไง

File → Build Settings → เช็คว่า `Level1` อยู่บนสุดและติ๊กอยู่ → Build
เกมออกมาเป็นหน้าต่าง 1280x720 (ไม่ใช่เต็มจอ) ปิดได้ด้วยปุ่ม X หรือกด Esc แล้ว Q
เล่นไฟล์ .exe ที่ได้ ต้องเล่นจบได้จริงนอก editor ไม่ใช่แค่ในโปรแกรม

## ถ้าเวลาไม่พอ ตัดตามลำดับนี้

1. เมนูหลัก → ใช้หน้าคำแนะนำนิ่งๆ แทน
2. อนิเมชันตัวละคร → ใช้ภาพนิ่งกลับด้านซ้ายขวาพอ (โค้ดกลับด้านให้อยู่แล้ว)
3. จักรยาน power-up → ลบออกจาก scene ได้เลย ระบบอื่นไม่พึ่งมัน
4. จำนวนกับดัก → ลดลง (ลดกับดักธรรมดาก่อน เก็บกับดัก troll ไว้)
5. เพลง/เสียง → ตัดทิ้งเลย (ฟอร์มเขียนว่า Optional และเรามี bell timer + death counter ครบ "more than one" อยู่แล้ว)

**ห้ามตัดเด็ดขาด:** เดิน/กระโดด/วิ่ง, ตัวนับเวลา, ระบบชีวิต, ระบบคะแนน, กับดัก troll, เงื่อนไขชนะ/แพ้
หกอย่างนี้เขียนไว้ในฟอร์มที่ส่งอาจารย์แล้ว ขาดอันไหนคือทำไม่ตรงที่รับปากไว้
